export default function LoadingState() {
  return (
    <div className="rounded-[28px] border border-white/10 bg-white/[0.04] p-8 text-center text-sm font-bold text-slate-300">
      Đang chuẩn bị lịch thi đấu. Nếu mạng chậm, app sẽ tiếp tục hiển thị dữ liệu gần nhất.
    </div>
  );
}
